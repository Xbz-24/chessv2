// src/player/ai/move_picker.rs

use crate::game::moves::Move;

pub struct MovePicker {
    // possible attributes
}

impl MovePicker {
    pub fn new() -> MovePicker {
        MovePicker {
            // initialize attributes
        }
    }

    pub fn pick_move(&self) -> Move {
        // logic to pick a move
    }
}

